# Keep-alive settings are generated in apex-runtime.conf by the entrypoint.
# The steady profile defaults to Off; set APEX_KEEP_ALIVE=1 deliberately for
# a measured high-traffic deployment.
Timeout 60
