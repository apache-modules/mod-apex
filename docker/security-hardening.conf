# Matches httpd.conf in the repo root.
# Hide Apache/module version details from clients (error pages, Server
# header) and disable TRACE (mitigates cross-site tracing/XST attacks).
ServerTokens Prod
ServerSignature Off
TraceEnable Off
